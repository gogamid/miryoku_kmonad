name: 'Build'
on:
  - workflow_dispatch
jobs:
  test:
    uses: ./.github/workflows/main.yml
    with:
      mode: '["build"]'
      alphas: '[
        "qwerty"
      ]'
      nav: '[
        "invertedt"
      ]'
      clipboard: '[
        "mac"
      ]'
      layers: '[
        "flip"
      ]'
      mapping: '[
        "default"
      ]'
      kmonad_os: '[
        "mac"
      ]'
